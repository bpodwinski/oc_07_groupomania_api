type Profile {
  id: ID!
  firstname: String!
  lastname: String!
  service: String!
  email: String!
  gravatar: String!
  role: String!
  posts: [Post]
}

extend type Query {
  profile: Profile
}

extend type Mutation {
  updateProfile(
    firstname: String
    lastname: String
    service: String
    email: String
    password: String
  ): Profile
}
