type Comment {
  id: ID!
  postId: ID!
  userId: ID!
  content: String!
  createdAt: Date!
  updatedAt: Date
  user: User!
  post: Post!
}

extend type Query {
  comment(id: ID!): Comment
  comments: [Comment]
}

extend type Mutation {
  createComment(postId: ID!, content: String!): Comment
}
